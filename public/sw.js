// Service Worker vacío para evitar errores 404
// Este archivo puede ser eliminado si no se necesita un Service Worker

self.addEventListener('install', function(event) {
  // No hacer nada
});

self.addEventListener('fetch', function(event) {
  // No hacer nada
});
